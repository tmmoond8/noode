import React from 'react';
import { Box, Flex, Stack, Wrap } from '@chakra-ui/react';

interface Props {
  title?: string;
}

export function BottomBar({ title }: Props) {
  return <Box h="40px">bottomBar</Box>;
}
